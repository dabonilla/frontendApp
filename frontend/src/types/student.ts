export type Student = {
    id: number;
    name: string;
    lastName: string;
    email: string;
    dateOfBirth: string;
};

export type StudentName = {
    studentId: number
    studentName: string;
}